<h1>BINGO BONGO TRAVEL SITE </h1>

<% @cities.each do |city| %>
<p> City Name: 
<%= city.name %>
</p>
<p>
<%= city.id %>
</p>
<%= image_tag city.photo_url %>
<%= button_to city.name, city_posts_path(city), {class: 'btn btn-info'}%>
<% end %>