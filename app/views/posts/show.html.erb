<p>
    <strong>Title:</strong>
    <%= @post.title%>
    </p>

    <p>
    <strong>Review:</strong>
    <%= @post.review%>
</p>

<%= link_to 'Edit Post', edit_city_post_path, {class: 'btn btn-default'}%>
<%= link_to 'Destroy', city_post_path(@city, @post), method: :delete, :class => 'btn btn-danger', data: { confirm: 'Are you sure?'} %>
